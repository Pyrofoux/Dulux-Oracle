

Fix:


Todo:
+ Animation shuffle deck - slow rotation?

+ Mobile support (interactions, viewport)
+ Better grid snaping
+ Info bubble with:
  + Controls (right click, left click, drag)
  + Options  (snap to grid, shadows...)
  + Example of spreads (Button to automatically make one?)